{
  "task": "Add adapter smoke tests",
  "objective": "Add minimal tests for stdio and HTTP adapters to ensure entrypoints respond correctly.",
  "rules": [
    "Add test_stdio_loop_smoke in tests/test_stdio.py",
    "Simulate one JSON-RPC request (list_schemas) to mcp.stdio_main and assert valid response",
    "Add test_http_app_smoke in tests/test_http.py",
    "Guard HTTP test with pytest.importorskip('fastapi')",
    "Use FastAPI TestClient to hit a basic route and assert JSON shape",
    "Keep tests minimal, no new fixtures or deps"
  ]
}
