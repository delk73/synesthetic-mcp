{
  "task": "Fix CI import hack with editable install",
  "objective": "Replace PYTHONPATH= hack in CI with pip install -e ., adding a minimal packaging stub if required.",
  "files": [
    ".github/workflows/ci.yml",
    "requirements.txt",
    "mcp/__init__.py"
  ],
  "constraints": {
    "rules": [
      "Remove PYTHONPATH override from CI workflow.",
      "Add pip install -e . step before running pytest.",
      "If no packaging metadata exists, create a minimal setup.py at repo root with setuptools.setup and find_packages for mcp/* only.",
      "Do not add extraneous metadata (authors, classifiers, etc.).",
      "Keep requirements.txt as the single source of runtime/test dependencies."
    ]
  }
}
