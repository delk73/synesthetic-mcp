{
  "task": "Cleanup MCP v0.2.5 state",
  "objective": "Update docs and README to reflect v0.2.5 as current, and deduplicate backend tests for clarity.",
  "changes": [
    {
      "id": "spec_version_update",
      "description": "Promote v0.2.5 from 'next' to stable in docs/mcp_spec.md.",
      "edits": [
        "docs/mcp_spec.md:1-2 — change heading to '# MCP Spec (v0.2.5)'.",
        "docs/mcp_spec.md:167-215 — fold the 'Scope (v0.2.5 — Next)' section into the main body, removing 'Next' wording."
      ]
    },
    {
      "id": "readme_version_update",
      "description": "Update README front matter to show v0.2.5 instead of 0.1.0.",
      "edits": [
        "README.md:1-4 — change version string to 'v0.2.5' and align with current release."
      ]
    },
    {
      "id": "dedupe_backend_tests",
      "description": "Remove duplicate definition of test_backend_assets_path_normalization.",
      "edits": [
        "tests/test_backend.py:46-57 — delete duplicate function body.",
        "tests/test_backend.py:170-181 — keep only one definition to preserve coverage."
      ]
    }
  ],
  "constraints": {
    "rules": [
      "Docs must clearly mark v0.2.5 as the current spec, not 'next'.",
      "README version badge/front matter must match v0.2.5.",
      "Only one definition of test_backend_assets_path_normalization remains.",
      "No functional logic changes outside docs/tests cleanup."
    ]
  },
  "exit_criteria": [
    "docs/mcp_spec.md header shows v0.2.5, no dangling 'next' section.",
    "README front matter shows v0.2.5 consistently.",
    "Duplicate test removed; pytest shows only one test_backend_assets_path_normalization.",
    "All tests green, audit shows no divergences."
  ]
}
